{
  "name": "davidaruck-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "audit": "node scripts/audit-static.mjs",
    "check": "npm run audit && node --check src/index.js && node --check src/chapter-enhancements.js && node --check public/assets/site.js && wrangler deploy --dry-run --outdir .wrangler/dry-run"
  },
  "devDependencies": {
    "wrangler": "4.131.1"
  }
}
